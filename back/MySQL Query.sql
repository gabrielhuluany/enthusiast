/* Execute esta consulta no MySQL Workbench ou no MySQL Command Line Client. */
/* É necessário criar o banco de dados antes de executar o projeto spring. */
CREATE DATABASE db_enthusiast;
/* As tabelas e colunas são criadas automaticamente ao executar o projeto spring. */

/* Recomendamos que utilize o usuário 'root' do MySQL para executar o projeto, pois terá os privilégios necessários. */
/* Caso seja necessário criar outro usuário do MySQL execute os comandos abaixo. */
/*
USE db_enthusiast;
CREATE USER 'A3User' IDENTIFIED BY 'user123';
SELECT USER FROM mysql.user;
GRANT ALL ON db_enthusiast TO 'A3User';
SHOW GRANTS FOR 'A3User';
*/
/* Lembre-se de alterar os dados de acesso ao MySQL no application.properties! */